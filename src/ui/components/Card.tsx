import {View, Text, StyleSheet} from 'react-native';
import React from 'react';

const Card = ({value}) => {
  console.log('bla card', value);

  return (
    <View style={{}}>
      <Text>Card</Text>
    </View>
  );
};

export default Card;
